# P1 golden set 占位

P1 质量验收（需真实 DeepSeek Key，连续 3 篇按量规打勾，3 篇中 ≥2 篇合格）通过后，
固定 5 个代表性 topic 的生成产物快照到本目录（title / content / tags / meta），
作为后续改 Prompt 或换模型时的回归基线。

mock 模式下不产出 golden set（产物是固定 JSON，无快照价值）。
