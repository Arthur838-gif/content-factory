# 项目进度快照（2026-08-20 晚）

> 承接 `progress-2026-08-18.md`。本文记录本周三批交付，以及**全部挂起项与恢复路径**——
> 用户拍板：小红书部分先到这里，挂起项下周再逐一开工。

## 一、本周交付（已提交，全量回归通过）

| 提交 | 内容 |
| --- | --- |
| c206363 | P5c 模型配置页：`/models` 维护多套大模型配置，文案/图片各设一个「当前使用」，页面切换下一次生成即生效；`.env` OPENAI_* 降级为回退默认；单价按模型归因，key 只回掩码 |
| 9d3b82d | P5d 工作台灵感选题区「采样一轮」按钮：直触 RedFox 七日爆款采样（入队 → 轮询 → 完成整页刷新；防重复计费、熔断提示），不用跳 /viral |
| 08fca48 | P8a 小红书链路增强（详见 README「小红书链路增强」节）：<br>① 素材正文摘录（RedFox `raw.article.desc`）进 evidence 快照与 `reference_points`，提示词从「标题（链接）」升级为「标题（链接）：正文摘录」；GitHub 行 desc 已含在标题里自动去重；顺修老 bug（rstrip 剥掉链接右括号）<br>② XhsNote 平台硬上限：标题 ≤20 字、正文 ≤950 字（+标签行 ≤1000），超限生成失败进重试自纠<br>③ 违禁词体检：xhs ready 文章页按钮（confirm 后才调 RedFox，按调用计费）→ 命中词逐个「＋词」回填本地 `sensitive_xhs.txt`（`POST /api/sensitive/{platform}/words`，文件追加即时生效）；英文子串误报（av ⊂ Gravitas）按 skill 同款规则剔除<br>④ 真实验证：文章 37 体检连通，检出「绝对 / 推荐」两个行业禁用词 |
| c6e0afe | （上周收尾）xiaohongshu-mcp 降级源废弃，RedFox 成为唯一采样源 |

小红书链路现状自查结论：**代码层面已完整**（采样→打分建题→栏目排期→文案→出图→素材包→回填全链有实现有测试）；挂起项集中在「开关没打开」「运营动作没发生」「少量代码缺口」，见下。

## 二、挂起项清单（下周做，按优先级）

### A. 用户拍板 / 一行改动

1. **生图总闸未开**：`.env` 里 `CF_IMAGEGEN_ENABLED=false` → 两段式 AI 封面
   （LLM 归纳提示词 → cogview-4 底图 → PIL 叠字）从未生效，封面一直是纯色版式。
   启用路径：`.env` 删掉该行或改 `true` + 重启 uvicorn。/models 页暂无图片配置——
   会回退 env（`GLM_IMAGE_MODEL=cogview-4` + 同一 bigmodel Key），开闸即用；
   想精细管理再在页面补一条图片配置。成本约 ¥0.06/张。
   **（用户已明确：生图最后再定。）**
2. **周度拆解从未跑**：`RUN_SCHEDULER=0`，调度器整体关着。现状：13 条 viral_samples
   的 reason 全是 `rule`、tag_library 0 条——「标题模式/情绪词/结构沉淀」半边价值未兑现。
   快动作：/viral 页点一次「立即周度拆解（调试）」。开调度器也安全：xhs 定时采样
   独立开关默认 false，不会偷跑付费采样；唯一噪音是无数据源的热榜采集器失败熔断（已知）。

### B. 运营动作（代码不缺）

3. **发布回填 0 篇**（xhs ready 已囤 16 篇）：P4 数据飞轮没转过。发布 2–3 篇并在
   预览页回填阅读/点赞/收藏，回填驱动评分、模板效果分、阈值校准视图即启动。
4. **敏感词表冷启动**：`data/sensitive_xhs.txt` 仍只有几条占位词（13 行）。人工按
   「所涉领域监管词 + 平台违禁词」整理 100–200 条；体检命中词回填是加速器不是替代。
5. **GLM 凭据轮换**：若 `.env` 的 OPENAI_API_KEY 是早前聊天泄漏过的那个，作废换新。
6. **Golden Set 固化**：`tests/golden/` 目前只有 README 占位。真实 Key 连续 3 篇过
   P1 量规后快照固化。
7. **阈值校准**：回填数据累积后按 `docs/p4-calibration.md` 流程拍板。当前
   VIRAL_FANS_MAX=5000 / VIRAL_SCORE_MIN=2.0 / VIRAL_LIKES_MIN=500 仍是初始值。

### C. 代码缺口

8. **手动建题入口**：`POST /api/topics` + 工作台表单（source=manual）未实装——
   已确认要做；DB 里的 manual 行是当时冒烟测试直插的，没有 API。
9. **热榜采集无数据源**：RSSHub 自建中止（见 progress-2026-08-18.md §5）。恢复：
   拉 `chromium-bundled` 镜像 + 查 zhihu/baidu 新路由名 + 改 `config.HOTBOARD_SOURCES`
   + `.env` 指自建实例；或换其他热榜 API。
10. **次要**（不阻塞使用）：工作台灵感池 / 文章列表无分页；radar.py 过时 TODO；
    生成新一批选题后才吃得到素材摘录（存量 evidence 无 desc，兼容但不含）。

## 三、当前线上状态备忘

- 服务：uvicorn 127.0.0.1:8000（后台任务常驻），工作树 clean，HEAD=08fca48。
- 模型配置：文案 GLM-4.7（页面 active）；图片走 env 回退；`CF_LLM_MOCK=0`。
- 数据：hot_items 276（全在近 7 天）、viral_samples 13、radar 选题 new 10 / used 3、
  pillar 选题在排 4、xhs ready 16 / published 0。

## 四、P9 公众号数据链路（同日追加，已提交）

用户拍板「接下来对微信公众号部分做优化」并下载了 12 份 RedFox 公众号 API 文档
（`redfox-api文档/公众号/`，不入库）。接口选型结论：**只接优质库 2 个只读接口**
（searchArticle sortType=_4 最热 / queryArticleDetail 按_url 抓详情），账号维度
3 个、广域库 5 个、AI 创作搜索 2 个不接（推迟项见下）。

交付内容（细节见 README「P9 公众号数据链路」节与 deliverables.md）：

- 采样：`gzh_sample` 采集器进统一队列（手动触发；定时仍仅 xhs）；
- 判定：阅读量口径（reads≥10000 且互动密度≥0.08），gzh 绝不走 xhs 判定
  （fans=0 除以 1 全数误判的坑，有守卫用例锁死）；
- 手动喂样本：贴 URL → 详情接口抓全量指标（1 次计费，查重在计费前）；
- 生成：WechatArticle 30/54/3000 字硬上限、wechat_article.yml v2（系列+标签+写作
  规范）、标题打分 wechat 四维 0-100 制；
- 发布：PIL 封面 900×383（零计费）+ 文稿素材包（含 digest.txt）、体检平台参数化
  （wechat → 微信公众号词库）、回填表单公众号六字段、效果分 +watches×1 +shares×3；
- 测试：test_gzh.py 32 项 + 全量回归 13 套/86 项全绿。

**真实接口验收（已执行，合计有效计费 3 次 + 未命中查询不扣费）**：

- 首轮两遇 RedFox story API 全线 502（含此前实测可用的小红书违禁词端点——
  纯服务端故障、站点本身 200，与参数无关，wechat 体检**不回滚**）；第三轮恢复。
- `searchArticle` **可用**：sortType=_4 通过，20 条，readCount/watchCount/shareCount/
  likeCount/collectCount/commentCount/isOriginal/workUuid 字段齐全（首条 readCount=3793，
  低于 10000 入选阈值——正常落 hot_items 不建题）。采样链路成立。
- `queryArticleDetail` **接口通、覆盖有限**：对搜索返回的 6 条 `__biz` 长链全部返回
  业务码 3203「优质库暂未收录该内容，积分未扣除」（明确不扣费）。端点鉴权与
  协议正常，是数据覆盖面问题。手动喂样本遇到时 502 透传该报错。
  后续两条补救路径已记推迟项：广域库 fallback（厂商建议）、按 workUuid 查详情
  （搜索结果自带 uuid，接口文档已备）。
- `sensitive_word_search(platform=微信公众号)` **可用**：检出「免费领」、
  分类「敏感词」——wechat 体检正式开放，无需回滚。
- **用户短链实测（08-20 下午追加）**：用户提供的真实分享短链
  `https://mp.weixin.qq.com/s/XvbZbYt0C8qEAvwMY4qzTg`（公众号「月夜烛峰」AI 工具文）
  走 `/api/viral-samples/gzh-manual` 全链路，两次均约 35 秒返回业务码 3203
  「优质库暂未收录，积分未扣除」——**「短链形态可用」假设证伪**，此前 6 条长链
  3203 与 URL 形态无关，纯属账号覆盖面：优质库只收腰部以上公众号近 30 天文章，
  小众账号的文章查不到。链路行为全部符合设计：查重在计费调用前、3203 → 502
  透传上游原文、零半成品写入（hot_items 无残留行）；viral.html 表单已补覆盖
  范围提示。
- **detail 端点稳定性备注（08-20 下午）**：同日一次调用出现 HTTP 200 响应头到达后
  响应体长时间滴流，handler 挂起 10 分钟以上（`httpx.Timeout(180)` 按单次 socket
  读计时，滴流不触发超时）；线程池隔离下其他请求不受影响，最终重启服务清掉挂起
  请求（写事务从未开启，无半成品风险）。重试即恢复正常（3203 约 35 秒返回）。
- 报告：`tests/_run_real_gzh_acceptance.py`（可重跑，结论落输出目录）。

### P9 推迟项（后续按需开工）

1. **账号维度追踪**：searchUser / queryWorkList / queryUser——含 redfoxIndex
   账号规模，可给 gzh 判定加「腰部以下账号修正」（对齐 xhs 低粉口径的另一半）。
2. **广域库 fallback**：优质库搜不到的词 / 用户贴链查不到详情时的兜底（当前
   直接空结果 / 502）。用户短链实测证伪「短链形态可用」假设后，这是小众账号
   文章取数的主要补救路径（厂商 3203 报错信息也明确指向广域库）。
3. **AI 创作搜索**（queryAiMsgs 等 2 个）：与现有生成链路重叠，价值待评估。
4. **gzh 定时采样**：当前只手动（计费纪律）；需要时加 scheduler 独立开关。
5. **周度拆解 gzh 化**：xhs_teardown 只拆 xhs 样本；gzh 样本量起来后再拆。
6. **gzh 阈值校准**：GZH_READS_MIN=10000 / GZH_SCORE_MIN=0.08 是初值，
   首轮真实采样后按分布复核（流程同 p4-calibration.md）。
7. **gzh 深采翻页**：searchArticle 每关键词只取第 1 页 20 条（offset 翻页已留接口）。

## 五、P9 补丁：公众号采样 × 栏目联动（08-20 晚）

用户问「公众号采样好以后怎么和内容栏目、工作台按钮联动」——排查发现两处断点并补齐：

- **素材池三源同吃**：`pillar.py _matched_pool` 原来只认 `("xhs", "github")`，
  公众号素材进不了「生成本周计划」与周主题规划的素材池。现加入 `"gzh"`：
  点赞当热度参与倒序，evidence 快照带阅读/在看/分享口径；gzh 条目标题未命中
  词池时靠采样词 `raw.keyword` 归队（与 xhs 同路径）。
- **栏目定向采样支持公众号**：`POST /api/pillars/{id}/sample?collector=gzh_sample`
  （默认仍 `xhs_sample`，白名单外 422）。去重键按采集器分开
  （`pillar:{id}` / `pillar:{id}:gzh_sample`）——小红书与公众号任务并行不挤占，
  同采集器连点仍复用在跑任务不重复计费；熔断按采集器检查。栏目页每行加
  「公众号采样一轮」按钮，confirm 明示每关键词计费 1 次。

联动全景（现在完整的一条链）：**栏目词池 →（工作台「公众号采样」全量 / 栏目页
「公众号采样一轮」定向）→ hot_items → 爆款判定建题进工作台「灵感选题」→
或直接进「生成本周计划」素材池 → 栏目排期选题（工作台「本周栏目排期」）→
两处的「生成公众号」按钮 → 文章页（四维打分 / 体检 / 封面素材包 / 发布回填）**。

测试：test_gzh.py +2（定向采样端点并行/去重/422、素材池 gzh 归队）共 34 项；
test_pillar.py 加 gzh 种子（赞 1200 进合集 evidence 挤掉最低赞）+ 3 项新检查；
全量回归 88 pytest + 13 脚本套件全绿。未提交（待用户确认）。
