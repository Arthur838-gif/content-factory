# 项目进度快照（2026-08-18 下午）

> 承接 `review-2026-08-18-fixes.md`（上午的七维评审）。本文记录当天下半场：
> mcp 部署、采样器适配、微信超时修复，以及全部未完成事项的清单与恢复路径。

## 一、当日新增进展

### 1. xiaohongshu-mcp 已部署（可用，等账号解封）

- 容器 `xiaohongshu-mcp`（xpzouying/xiaohongshu-mcp **v2.5.0**），端口 18060，
  `--restart unless-stopped`；健康检查 `/health` 通过，18 个 MCP 工具注册。
- 登录态持久化：cookie 绑定挂载到 `C:\YU\project\xhs-mcp-data\cookies.json`
  （仓库外，不进 git）；数据目录 `C:\YU\project\xhs-mcp-data`、图片目录
  `C:\YU\project\xhs-mcp-images`。
- 登录辅助脚本：`C:\YU\project\xhs-mcp-login.py`（拉二维码存 PNG + 轮询登录状态）。
- **实测结论**：
  - 工具名是 `search_feeds`（不是文档写的 `search_notes`），入参 `keyword`；
    返回 `{feeds:[{id, xsecToken, noteCard:{displayTitle, user, interactInfo}}]}`，
    **搜索结果不含作者粉丝数**。
  - `user_profile` 在 v2.5.0 上不可用：内部 60s deadline 必 panic。**禁止调用**
    （当天的账号风控疑似由连续两次 user_profile 长会话触发）。

### 2. 采样器代码已适配（未提交）

`app/collectors/xhs_sample.py`：
- `search_notes` → `search_feeds`（`_search` 与 `probe_fans` 两处）；
- `parse_search_notes` 兼容两种响应形状：真实 `noteCard` 嵌套（归一化函数
  `_normalize_note`）与旧扁平键（测试 fixture）；支持 `{feeds:[...]}` 包装展开；
  URL 缺省按 note_id 构造 explore 链接。
- `test_p1b.py` 全量回归 **PASS**（54 项，含熔断/撞题/人工喂样本）。

### 3. 小号被风控（阻塞项）

- 过程：登录成功 → search_feeds 成功 1 次 → 两次 user_profile 60s panic →
  session 被作废，重启容器后 check_login_status = 未登录，扫码也受限。
- 处置：停止重试登录（避免加深标记），**等 24–72h 后用
  `xhs-mcp-login.py` 重扫**；期间小红书侧走降级模式（`/viral` 人工喂样本）。

### 4. 微信长文超时已修复（验证通过）

- 现象：公众号生成（1500–3000 字）全部 `The read operation timed out`，
  小红书（几百字）正常。
- 修复：`.env` 加 `CF_LLM_TIMEOUT_SECONDS=240`。
- 实测：GLM-4.7 生成 2144 字正文耗时 **103.7s**（completion 3910 tokens，
  cost_est ≈ $0.002/篇），240s 余量足够。
- ⚠️ 需重启 uvicorn 生效（同时加载采样器新代码）。

### 5. RSSHub 自建：中止（可随时续）

- 公共实例（rsshub.app 及多个镜像）本机全部不可达。
- 自建尝试：`diygod/rsshub:latest` 已拉到本地（615MB），但该精简镜像缺
  Playwright 浏览器（微博路由 503），知乎/百度路由 404（新版路由名待查）；
  `chromium-bundled` 镜像未拉完即按用户要求中止。热榜采集暂无数据源。

## 二、未完成清单（按优先级）

### A-1. 内容栏目层（P5，当晚新增，✅ 已实测排满本周计划）

17. **pillar 栏目层已实装**（未提交）：解决"一次性选题无法支撑账号日更/周更"。
    - 数据：`pillars` 表（名称/固定角度/每周期数/关键词池/启停）；
      排期选题 `topics.source='pillar'`，**不参与 radar 撞题合并**（系列标题天然相似）；
    - 服务：`services/pillar.py` 周排期——周更固定档（标题带周期区间，幂等）+
      多期轮换档（每期绑一条素材、likes 倒序、同栏目素材不重复用）；
      素材匹配 = 标题 / 正文 desc / 采样词 raw.keyword 三途径；
    - 接线：`xhs_sample._queries` 优先取启用栏目关键词池；API `/api/pillars`
      （CRUD + `POST /api/pillars/plan`）；页面 `/pillars`（建栏目/停启用/生成本周计划）；
    - 栏目验证（RedFox 数据）：AI工具方向周素材池 ~500 篇（howto用好AI 240 +
      AI工具 159 + AIGC 102），周低粉爆款候选 5+，日更余量 70 倍；
    - **真实库已种子两个栏目**（用户拍板：合集周更 + 深挖多期合一，每周 5 期），
      本周计划已排满：合集 1 + 深挖 5 = 6 条 pillar 选题（#5、#6、#11-#14）；
    - 测试：`tests/test_pillar.py` 全过；test_p1b / test_redfox 回归 PASS。
    - **页面已按新流程重构**（P5 一起做的，8001 端口实例验证过）：
      导航按工作流排序（工作台·内容栏目·素材采样·模板管理·报表，当前页高亮）；
      `/` 选题台 → **工作台**：顶部概览卡（每栏目本周进度 已排/已生成/已发布 +
      采样引擎健康度 + 灵感池数量），正文分「本周栏目排期」（按栏目分组、
      展示固定角度）与「灵感选题」（一次性 radar/manual，按评分、过期标记）；
      `/viral` 改名「素材采样」（定位说明 + 与栏目/工作台互链）。
    - 待办：每周一早自动 plan（可挂 scheduler，当前手动点按钮）；
      生成提示词可按栏目定制模板（当前 angle 走通用 note 模板）；
      工作台灵感池无分页（topic 多了再切）。

### A0. RedFox 备用数据源（当晚新增，✅ 已实测跑通）

16. **RedFox（红狐数据 redfox.hk）双源采样已实装并验证**（未提交）：
    - `app/collectors/redfox.py` 新增：爆款洞察搜索（结果自带 authorFans，
      低粉爆款判定可直接跑通）+ 作品详情（全文/阅读量，CLI 手动用）；
    - `xhs_sample.py` 改双源：配 Key 走 RedFox，无 Key / 调用失败自动降级
      mcp；`probe` 测 RedFox、`probe-mcp` 测 mcp；
    - config 新增 `CF_REDFOX_API_KEY`（或环境变量 `REDFOX_API_KEY`）、
      `CF_REDFOX_WINDOW_DAYS`（默认 7）、`CF_REDFOX_TIMEOUT`（默认 30）；
    Key 已配入 `.env`。测试：`tests/test_redfox.py` 全过；`test_p1b.py`
    全量回归 PASS。**真实实测**：probe "AI工具" → 50 条全带 fans；
    一轮采样 fetched=50 → inserted=50 → **viral_created=3 / topics_created=3**
    （低粉爆款引擎首次端到端跑通，结论已回写 `docs/p-1b-fans-probe.md`）。
    - 剩余动作：**重启 uvicorn**（加载双源代码 + 240s 超时，与 A1 合并）；
      后续按调用量留意费用（每词每轮 1 次调用）。

### A. 用户动作（当天）

1. **重启 uvicorn**（加载采样器修复 + 240s 超时）：
   `Ctrl+C` 后 `.venv/Scripts/python -m uvicorn app.main:app --host 127.0.0.1 --port 8000`。

### B. 短期（1–3 天）

2. **小号解封后重登** → 跑 `python -m app.collectors.xhs_sample probe "AI工具"`
   → 确认 `fans_available`（预期仍为 false：搜索不含 fans）→ 跑一轮真实采样
   （先 `CF_XHS_SAMPLE_KEYWORDS` 限 2 个词）→ 把本文「实测结论」同步进
   `docs/p-1b-fans-probe.md`。
3. **提交代码**：`xhs_sample.py` 的 search_feeds 适配尚未 commit。

### C. 评审遗留（review-2026-08-18-fixes.md 之后的增量）

4. `.env` 配 `CF_LLM_PRICE_INPUT / CF_LLM_PRICE_OUTPUT` 为 GLM 官方价
   （当前成本报表按 DeepSeek 价估算，数字不可作账单依据）。
5. `git rm --cached tests/alert_received.log`（已被跟踪，.gitignore 不解跟踪）。
6. 人工喂样本 `fans` 允许 0 会算出失真爆文率（fans=0 按 1 计）——建议改 `ge=1`。
7. `tests/_run_real_acceptance.py` 未跟踪，决定入库或删除。
8. 次要：`radar.py` 过时 TODO、`sensitive.py/db.py` 类型标注、页面列表无分页、
   `recompute` 规模注释、hotboard 共享 httpx.Client 线程安全备注。

### D. 功能缺口 / 运营待办

9. **选题台无手动建题入口**（已确认要做：`POST /api/topics` + 选题台表单，
   source=manual）。
10. **热榜采集无数据源**：续上 RSSHub 自建（拉 chromium-bundled 镜像 + 查
    zhihu/baidu 新路由名 + 改 `config.HOTBOARD_SOURCES` + `.env` 指
    `http://localhost:1200`），或改用其他热榜 API。
11. P-1b 真实质量验收：连续 3 天采样/人工录入，`viral_samples ≥ 5`，人工抽查
    过半数"值得仿写"。
12. 首篇真实发布回填（验证选题台排序变化 + 成本报表与 usage 手工抽算一致）。
13. Golden Set 固化（真实 Key 连续 3 篇过 P1 量规后快照 `tests/golden/`）。
14. 阈值校准：回填数据累积后按 `docs/p4-calibration.md` 流程周四会拍板。
15. GLM 凭据轮换（早前聊天泄漏的 Key 作废——当前 .env 中 Key 若即该 Key，建议换）。

## A1 模板升级：xhs/note v2（栏目适配）

- 背景：v1 人设只有"情绪化共鸣"，与 AI工具栏目的教程/测评方向冲突；且 v1 不使用
  `reference_points` 变量，栏目选题携带的采样素材在生成时被静默丢弃（wechat/article
  v1 已有该条件块，note 没有）。
- 做法：按 SDD 设计走管理页路径——POST /api/prompts 创建 v2（id=4，enabled，
  选版取最高版本即自动生效，模板每次现读库故无需重启）。种子文件保持 v1 不动
  （test_p1 锁定 v1 幂等键；模板迭代的既定路径是库内版本化，不回写种子）。
- v2 变化：① system 双模式人设（情绪共鸣 / 干货教程自动切换），底线约束
  "工具名、数字、步骤必须有依据，素材里没有的不编造"；② user 增加
  `{% if reference_points %}` 参考素材块（含反抄袭指令）；③ content 字段指令
  增加编号列表组织方式。输出 JSON 契约不变。
- 验证：用本周真实栏目选题 #5/#6 渲染，确认选版 v2、素材 URL 进入提示词。
- 遗留：#5 合集 evidence 仅 1 条素材但标题承诺 5 个工具——生成前建议先补采样
  （多关键词跑一轮），否则模型可能凑数；后续可让排期器按实际素材数生成合集标题。
